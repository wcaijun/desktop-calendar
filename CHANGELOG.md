# 更新日志

## v1.1.0（2026-09）

**顶栏与视图**
- 顶栏精简为分组式布局：「＋ 新建学年」并入学年下拉，皮肤选择移入设置，「今天」并入视图切换组（校历 / 月 / 周 / 今天）。
- 新增「今天」单日视图：只显示当天课程（按时间排序）与待办 / 日程，可直接勾选、删除、添加。

**课表管理（校历设置 → 课表管理）**
- 支持课程的新增 / 修改 / 删除。
- 支持导入：Excel（.xlsx / .xls，兼容常见教务系统导出格式）、CSV、JSON。
- 支持导出：xlsx / csv / json。
- 导入非法或损坏文件时明确报错，且不会改动现有课表数据。
- 新增「打开数据文件夹」快捷入口，方便备份与手动编辑。

**周视图**
- 添加待办改为「双击星期X表头」弹出输入框；表格底部保留操作提示文案。

**修复与优化**
- 日期格式兼容增强：`2026-09-07`、`2026/9/7` 与 Excel 日期单元格均可识别。
- 窄窗口下的周视图排版优化（提示行免滚动可见、卡片文字不再逐字换行）。
- 顶栏在最小窗口宽度下不溢出、不换行。

**发布与体验**
- 新增《示例课表模板.xlsx》：方便新用户直接体验课表导入。
- 提供 Linux 版本（AppImage / tar.gz）与 macOS 自动构建支持。
- 公开版：界面页脚与图标采用中性标识。

## v1.0.0（2026-09）

- 首个版本：校历 / 月 / 周视图、待办事项（添加 / 完成 / 删除）、课表自动映射显示、桌面壁纸模式、5 款主题、可选印象笔记双向同步。
